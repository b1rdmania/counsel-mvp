// Feature flags for the app.
// Flip these as needed — they're read at build time.

// Demo mode: disables matter creation so users can only interact
// with the seeded demo matters which have been tested end-to-end.
export const DEMO_MODE = true;
